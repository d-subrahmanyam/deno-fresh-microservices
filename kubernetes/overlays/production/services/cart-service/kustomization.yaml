apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Production deployment - requires approval.
# Update newTag to an immutable image SHA before applying.
# Record: image tag, approver, rollback target in the release ticket.
# To deploy:
#   kustomize edit set image microservices/cart-service=your-registry/microservices/cart-service:git-<sha>
#   kubectl apply -k kubernetes/overlays/production/services/cart-service
resources:
  - ../../../../base/services/cart-service
images:
  - name: microservices/cart-service
    newTag: git-replace-me
