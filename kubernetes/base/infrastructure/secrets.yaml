# Placeholder for local and development use only.
# In staging and production replace this file with sealed-secrets or an
# external secrets operator. Never commit real credentials to this file.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: microservices
type: Opaque
stringData:
  DB_USER: postgres
  DB_PASSWORD: changeme
